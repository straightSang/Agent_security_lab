# Day 1 — Agent loop·상태 모델과 실습 헌장

| 필드 | 값 |
|---|---|
| 날짜 | 2026-08-12 |
| 커리큘럼 | W1 D1 |
| 상태 | 완료 |
| 저장소 | AI_security_Lab (구조 개편 전) |

## 오늘의 목표

모델 출력과 실행 권한을 분리한다.

## 구현

`Agent_v0.1.py`, `trace_logger.py`. agent state와 tool intent를 분리하고 PolicyDecision 없이는 도구를 호출하지 않도록 했다.

## 실험

notes/ 아래 inject.txt를 두고 무해한 파일이 '정책을 무시하라'고 말해도 실행 권한이 생기지 않는지 확인.

## 배운 것

제안(intent)과 실행(dispatch)은 다른 것이다. 이 분리가 이후 8일의 토대가 됐다.

## 산출물

Day1/EXP_LOG.md, Day1/questions.md

## 이후 개정에서 바뀐 것

이 날의 코드는 `AI_Agent_Security_Lab_v2/src/`로 통합되었다.
항목별 수정 내역은 `docs/CHANGELOG.md` 참조.

## 미해결 질문

- (작성)
