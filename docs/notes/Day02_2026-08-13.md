# Day 2 — 구조화 Logger·Trace 계약

| 필드 | 값 |
|---|---|
| 날짜 | 2026-08-13 |
| 커리큘럼 | W1 D2 |
| 상태 | 완료 |
| 저장소 | AI_security_Lab (구조 개편 전) |

## 오늘의 목표

모든 판정이 하나의 run_id로 연결되는 append-only 로그를 만든다.

## 구현

`Agent_v0.2.py`, `trace_logger.py`. JSONL 이벤트 스키마 확립. trust·capability·approval 필드를 스키마로 강제.

## 실험

agent_trace.jsonl에 run_id별 이벤트 체인이 끊기지 않는지 확인.

## 배운 것

관측 가능성이 없는 보안 주장은 완료되지 않은 것이다. 로그 스키마를 먼저 정한 것이 이후 evaluator를 가능하게 했다.

## 산출물

Day2/EXP_LOG.md, Day2/Threat Model v0.1.md

## 이후 개정에서 바뀐 것

이 날의 코드는 `AI_Agent_Security_Lab_v2/src/`로 통합되었다.
항목별 수정 내역은 `docs/CHANGELOG.md` 참조.

## 미해결 질문

- (작성)
