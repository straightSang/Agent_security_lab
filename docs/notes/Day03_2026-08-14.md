# Day 3 — Filesystem capability 정책

| 필드 | 값 |
|---|---|
| 날짜 | 2026-08-14 |
| 커리큘럼 | W1 D3 |
| 상태 | 완료 |
| 저장소 | AI_security_Lab (구조 개편 전) |

## 오늘의 목표

경로를 정규화해 sandbox 밖으로 나가지 못하게 한다.

## 구현

`Agent_v0.2.1.py` → `Agent_v0_2_2.py`, `authorization.py`, `permission.py` 분리. `safe_resolve()`로 경로 정규화 후 sandbox 결속.

## 실험

`..`, 절대 경로, 심볼릭 링크 조합으로 sandbox 탈출 시도.

## 배운 것

경로 검사는 문자열이 아니라 정규화된 결과에 해야 한다. resolve() 이후 relative_to()로 검사하는 패턴을 확정.

## 산출물

Day3/EXP_LOG.md, Day3/Threat Model v0.2.md, Day3/permission_policy.md

## 이후 개정에서 바뀐 것

이 날의 코드는 `AI_Agent_Security_Lab_v2/src/`로 통합되었다.
항목별 수정 내역은 `docs/CHANGELOG.md` 참조.

## 미해결 질문

- (작성)
