# Day 9 — MCP tool schema·least privilege

| 필드 | 값 |
|---|---|
| 날짜 | 2026-09-01 |
| 커리큘럼 | W2 D4 |
| 상태 | 완료 |
| 저장소 | AI_security_Lab (구조 개편 전) |

## 오늘의 목표

작업별로 노출하는 도구 자체를 최소화하고, 인자 계약을 서버에서 다시 검증한다.

## 구현

`security/tool_schema.py` 신설. MCP catalog + 프로필 3종(read_only / write_enabled / legacy_compat). `validate_tool_schema()`를 Runtime 최전단에 배치.

## 실험

D9-E01~E06 본 실험 + E07~E09 우회 회귀 + D7·D8 회귀. gate 호출 횟수를 mock으로 계측해 fixture의 expected.gate_calls와 대조.

## 배운 것

annotations는 표시용 힌트일 뿐 권한이 아니다. 그리고 조기 차단은 성능이 아니라 공격 표면의 문제다.

## 산출물

Day9/lab/results/D9_EXPERIMENT_REPORT.md, Day9/lab/ThreatModel0.6.md

## 이후 개정에서 바뀐 것

이 날의 코드는 `AI_Agent_Security_Lab_v2/src/`로 통합되었다.
항목별 수정 내역은 `docs/CHANGELOG.md` 참조.

## 미해결 질문

- (작성)
